# Changelog

نسخه‌ی جدید همیشه بالای این لیسته. شماره‌ی نسخه توی این ۴ جا باید یکی باشه:
`src/config/version.ts` ← `package.json` ← این فایل ← اسم فایل zip.
(همین شماره روی دکمه‌ی آخر پنل ادمین هم نمایش داده میشه.)

## v1.2.0

### وریفای (یکپارچه‌سازی + اصلاح روند)
- فقط **دو حالت** وجود داره: «وریفای/تایید شده» یا «تایید نشده». یه فیلد `verified` برای همه‌جا (درخواست کاربر، دکمه‌ی ادمین، لیست، پروفایل، اعلان‌ها).
- پروفایل کاربر وریفای‌شده: کنار اسم **تیک سبز** ✅ و به‌جای «تایید نشده» نوشته میشه «این کاربر مورد اعتماد می‌باشد و توسط ادمین تایید شده». (ایموجی پریمیومش همون `EMOJI_PREMIUM_VERIFIED_BADGE` هست؛ اگه خالی باشه ✅ معمولی.)
- دکمه‌ی «درخواست وریفای»: **فقط** زیر «ویرایش پروفایل» (داخل پیام پروفایل، فقط برای وریفای‌نشده‌ها) هست؛ از منوی ویرایش برداشته شد. کاربر وریفای‌شده دیگه نه دکمه رو می‌بینه نه می‌تونه درخواست بده.
- روند جدید: نمایش قوانین + عکس نمونه ← **تا «موافقم» نزده، ربات عکس/شماره قبول نمی‌کنه** ← عکس ← شماره فقط با دکمه‌ی **اشتراک‌گذاری شماره (Share Contact)** (شماره‌ی تایپی قبول نیست و شماره باید مال همون اکانت باشه) ← عکس + شماره با هم برای ادمین ← تایید ← پیام تبریک به کاربر (اعتماد بیشتر کاربران و ...) + اضافه شدن به «✅ کاربران وریفای».
- **باگ مهم:** عکس وریفای (و عکس مدرک گزارش) به‌جای رفتن به ادمین، توسط آپلودر «عکس پروفایل» گرفته و عملاً به‌عنوان عکس پروفایل ثبت می‌شد. اصلاح شد.
- درخواستِ نیمه‌کاره بعد از ۳۰ دقیقه خودکار فراموش میشه.

### دکمه‌ی «لغو» و گیر کردن توی مرحله‌ها
- همه‌ی مرحله‌هایی که ربات منتظر متن/عکس کاربره (بیو، استان، شهر، موقعیت، درخواست تغییر نام/سن، وریفای، گزارش، انتقال رلیک، پیام ناشناس و مراحل پنل ادمین) حالا یه دکمه‌ی شیشه‌ای **قرمز «❌ لغو»** دارن؛ با زدنش مرحله کنسل میشه و کاربر آزاده.
- اگه کاربر وسط یه مرحله روی یکی از دکمه‌های منوی اصلی (مثلاً «دعوت دوستان») یا یه دستور (/start) بزنه، مرحله خودکار لغو میشه و همون دکمه کار می‌کنه (قبلاً ارور «سن باید عدد باشه» می‌داد). برای ادمین هم زدن هر دکمه‌ی پنل، مرحله‌ی قبلی رو لغو می‌کنه.
- همزمان فقط یک مرحله‌ی «منتظر متن» فعاله.

### آیدی ناشناس یکپارچه
- «@» از جلوی آیدی ناشناس **همه‌جا** حذف شد (پنل ادمین، وریفای، انتقال رلیک، گزارش، ...). فقط شکل `user_xxxxxx` نمایش داده میشه. (یوزرنیم واقعی تلگرام هنوز @ داره چون یوزرنیمه.)

### لینک ناشناس من
- دکمه‌ی «لینک ناشناس من» حالا لینک شخصی `t.me/<bot>?start=msg_<آیدی>` می‌سازه (با دکمه‌ی اشتراک‌گذاری).
- کاربر ۲ با لینک میاد (اگه ثبت‌نام نکرده اول ثبت‌نام می‌کنه، اگه عضو کانال‌های جوین اجباری نیست اول عضو میشه) و پیام می‌نویسه ← کاربر ۱ می‌گیره «کاربری بصورت ناشناس پیامی برای شما ارسال کرد» + دو دکمه‌ی شیشه‌ای **پاسخ دادن** و **خوندم**. «خوندم» ← به فرستنده اعلان «خوانده شد» میره. «پاسخ دادن» ← جواب برای فرستنده میره (با همون دو دکمه، پس مکالمه ادامه پیدا می‌کنه).
- محدودیت: فقط متن (حداکثر ۱۰۰۰ کاراکتر)، حداکثر ۵ پیام در ۱۰ دقیقه به یک نفر. هویت فرستنده برای گیرنده مخفیه ولی برای پیگیری تخلف توی دیتابیس ذخیره میشه.
- `BOT_USERNAME` (env، اختیاری، پیش‌فرض NavaChatBot) برای ساخت لینک‌ها.

### رلیک کوین ← خرید با درگاه
- به‌جای «پکیجی تنظیم نشده»، یه متن + **۵ دکمه‌ی شیشه‌ای با قیمت پیش‌فرض** نمایش داده میشه (۱۰ رلیک=۵۰ هزار، ۲۵=۱۰۰ هزار، ۵۰=۱۸۰ هزار، ۱۰۰=۳۲۰ هزار، ۲۵۰=۷۰۰ هزار تومان). از «💳 تنظیم قیمت رلیک» توی پنل ادمین عوضشون کن.
- تا درگاه/ولت وصل نشده، زدن روی هر پکیج پیام «درگاه هنوز فعال نشده» میده. بعداً با `WALLET_BOT_USERNAME` (env) به ربات ولت وصل میشه.

## v1.1.1
- دکمه‌ی شیشه‌ای **«درخواست وریفای»** حالا زیر دکمه‌ی «ویرایش پروفایل» توی خودِ پیام پروفایل هم میاد (برای کاربرِ وریفای‌نشده؛ بعد از وریفای مخفی میشه). همون دکمه توی منوی ویرایش هم هست. ایموجی‌اش از `EMOJI_PREMIUM_VERIFY_REQUEST` میاد و اگه خالی باشه 🔵 معمولی.

## v1.1.0

### ایموجی‌ها (باگ اصلی)
- **علت باگ:** ایموجی پریمیوم یه تگ `<tg-emoji ...>` هست که تلگرام فقط وقتی می‌فهمه که پیام با `parse_mode: HTML` رفته باشه، و **هیچ‌وقت داخل متن دکمه** کار نمی‌کنه. چندین جای ربات یکی از این دو رو فراموش کرده بود (بنر دعوت، پیام دوم دعوت، صفحه‌ی رلیک، پیام تایید/رد وریفای، پروفایل کامل ادمین، دکمه‌ی «درخواست وریفای» و ...).
- **راه‌حل ریشه‌ای:** همه‌ی درخواست‌های خروجی از `src/ui/telegramSafety.ts` رد میشن: اگه تگ پریمیوم توی متن باشه خودکار HTML فعال میشه (و بقیه‌ی متن escape میشه)، توی دکمه به `icon_custom_emoji_id` منتقل میشه.
- **پاک کردن ID پریمیوم از env:** هر `EMOJI_PREMIUM_*` رو خالی کنی، همون‌جا ایموجی معمولی میاد (متن + دکمه + متن‌های ذخیره‌شده توی دیتابیس). کلید کلی `PREMIUM_EMOJI_ENABLED=false` همه‌ی پریمیوم‌ها رو یه‌جا خاموش می‌کنه. ID فقط‌عددی یا حتی کل تگ کپی‌شده هم قبول میشه.
- دکمه‌هایی که ID پریمیوم ندارن قبلاً بدون ایموجی می‌موندن؛ حالا ایموجی معمولیِ جایگزین جلوی متنشون میاد (منوی اصلی هم طوری اصلاح شد که با ایموجی جلوش هم تشخیص داده بشه).
- تیک وریفای/❌: قبلاً برای کاربر «تایید نشده» هم از ایموجی پریمیومِ تیک آبی استفاده می‌شد؛ حالا ❌ همیشه معمولیه.

### وریفای
- **تیک آبی کنار اسم** توی پروفایل برای کاربر وریفای‌شده اضافه شد.
- **اعلان وریفای / لغو وریفای با عکس** حالا برای وریفای از پنل ادمین (دکمه‌ی متنی و دکمه‌ی شیشه‌ای داخل پروفایل کامل) هم فرستاده میشه (قبلاً هیچ اعلانی نمی‌رفت). عکس لغو وریفای: `VERIFY_REVOKED_PHOTO`. اگه ارسال عکس خطا بده، همون متن بدون عکس فرستاده میشه (قبلاً خطا بی‌صدا قورت داده می‌شد و هیچی نمی‌رفت).
- صفحه‌ی «قوانین وریفای» بیش از ۱۰۲۴ کاراکتر بود و وقتی عکس نمونه ست شده بود، تلگرام کپشن رو رد می‌کرد و کل صفحه خطا می‌داد؛ حالا عکس جدا و متن قوانین جدا فرستاده میشه.
- دکمه‌ی جدید پنل ادمین: **«✅ کاربران وریفای»** ← تعداد + دکمه‌ی «مشاهده کاربران وریفای» (لیست صفحه‌بندی‌شده).

### دیدن اینکه کی پروفایلت رو دیده
- جستجوی آیدی نوا (وقتی کاربر توی چت نیست): پروفایل مثل قبل برای کاربر ۱ میاد؛ کاربر ۲ به‌جای پیام «مخاطب شما...» یه پیام ناشناس با دکمه‌ی شیشه‌ای «پرداخت» می‌گیره. با پرداخت ۱۰ رلیک (`PROFILE_VIEW_REVEAL_COST`) پروفایل کاربر ۱ نمایش داده میشه. موجودی کم → پیام «موجودی کافی نیست» + دکمه‌ی خرید رلیک. پرداخت اتمیک و ضد دوبار-کسر هست.
- پیام قدیمیِ «مخاطب شما...» فقط برای دکمه‌ی «پروفایل مخاطب» وسط چت باقی موند.
- جستجوی آیدی توسط ادمین/مالک بی‌صداست (برای تست: `PROFILE_VIEW_NOTIFY_ADMIN_LOOKUPS=true`).

### باگ‌های دیگه
- `nickname`/`bio`/اسم تلگرام و ... توی پیام‌های HTML escape نمی‌شد؛ یه `<` توی بیو کل پروفایل رو می‌کشت. اصلاح شد.
- Stars: `pre_checkout_query` توی `allowed_updates` وبهوک نبود ⇒ خرید با استارز تایم‌اوت می‌شد. **بعد از دیپلوی یه بار `npm run set-webhook` بزن.**
- تایید/رد عکس پروفایل فقط برای `ADMIN_IDS` کار می‌کرد (اگه فقط `OWNER_ID` ست بود آپلود عکس خطا می‌داد). حالا مالک + ادمین‌های پنل هم هستن.
- پیام پین پیش‌فرض (اگه از پنل عوض نشده بود) لینک‌ها و اسم «هایپر گپ» ربات قبلی رو داشت؛ یکی شد.
- دکمه‌ی «راهنما» و «پیشنهادات و انتقادات» هیچ کاری نمی‌کرد؛ حالا راهنمای شماره یک (قابل ویرایش از پنل) و آیدی پشتیبانی رو نشون میدن. «ویرایش راهنمای شماره یک» هم واقعاً روی مرحله‌ی معرفی اثر می‌ذاره.
- متن‌ها: «هایپر گپ» → «نوا» و ۳۰ رلیک → ۲۰ رلیک (مطابق پاداش واقعی).
- گزارش کاربر با دلیل خیلی طولانی (کپشن بالای ۱۰۲۴) دیگه ارسال به ادمین رو خراب نمی‌کنه.
- لیبل منوی اصلی برای زبان‌های en/ar (که هنوز متن ندارن) دیگه کرش/تداخل نمی‌سازه.

### ورژن
- دکمه‌ی آخر پنل ادمین (`🏷 نسخه 1.1.1`) هیچ کاری نمی‌کنه و فقط نسخه رو نشون میده.

## v1.0.0
- نسخه‌ی اولیه.
